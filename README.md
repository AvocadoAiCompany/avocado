# Project

## GitHub Pages Setup

To enable the documentation site:

1. Open the repository on GitHub.
2. Go to **Settings → Pages**.
3. Under **Source**, choose branch `main` and folder `/docs`.
4. Click **Save**.

The site will be available at `https://<org>.github.io/<repo>/` with no build step required.
